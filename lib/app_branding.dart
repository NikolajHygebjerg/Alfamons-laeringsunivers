/// Vist appnavn for brugere (titel, launcher, PWA m.m.).
///
/// Uændret af hensyn til butikker/migration: [pubspec.yaml] `name`, Android
/// `applicationId`, iOS/macOS `PRODUCT_BUNDLE_IDENTIFIER`, samt Windows-filnavn
/// `Alfamons.exe` (`BINARY_NAME` i [windows/CMakeLists.txt]).
const String kAppDisplayName = 'Alfamons læringsunivers';
