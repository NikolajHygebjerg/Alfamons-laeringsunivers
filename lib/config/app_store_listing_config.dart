/// **App Store (kun tal)** for «Åbn i App Store» / anmeldelsesside.
/// Findes i App Store Connect under **App information → Apple ID** (eksempel: `1234567890`).
/// Lad være tom, indtil ID er udfyldt — i så fald bruges i stedet den indbyggede
/// stjerne-pop-up (`requestReview`) på iOS, hvis systemet viser den.
const String kIosAppStoreAppleId = '';
